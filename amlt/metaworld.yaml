description: >
  Metaworld T=300 baselines: 2 envs (ML10, ML45) x 6 baselines x seed=0 = 12 jobs.
  n_layer=1 (override metaworld default of 2). V100 32GB on msrresrchlab.

target:
  service: sing
  workspace_name: wsgcrrbt
  name: msrresrchlab

env_defaults:
  BASE_DIR: /mnt/v-hihwang/projects/mate

storage:
  my_storage:
    storage_account_name: azsussc
    container_name: v-hihwang
    mount_dir: /mnt/v-hihwang

environment:
  image: amlt-sing/acpt-torch2.7.1-py3.10-cuda12.6-ubuntu22.04
  setup:
    - conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main || true
    - conda create -y -n mate python=3.10
    - conda run --name mate conda install -y -c conda-forge mesa libstdcxx-ng
    - conda run --name mate pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu126
    - conda run --name mate pip install -r requirements.txt

code:
  local_dir: $CONFIG_DIR/..

jobs:
  # ============================== ML10 ==============================
  - name: ml10_mate_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: &common_env
        AMLT_DOCKERFILE_TEMPLATE: default
        SHARED_MEMORY_PERCENT: 0.5
        WANDB_API_KEY: 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
        WANDB_ENTITY: mate_research
        PYTHONUNBUFFERED: "1"
        OMP_NUM_THREADS: "8"
        MKL_NUM_THREADS: "8"
    command:
      - &setup_cmd "export LD_LIBRARY_PATH=/home/aiscuser/.conda/envs/mate/lib:$$LD_LIBRARY_PATH"
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML10/ml10_mate_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML10 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/mate_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml10_mate_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml10_gpt_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML10/ml10_gpt_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML10 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/gpt_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml10_gpt_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml10_lstm_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML10/ml10_lstm_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML10 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/lstm_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml10_lstm_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml10_splagger_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML10/ml10_splagger_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML10 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/splagger_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml10_splagger_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml10_oracle_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML10/ml10_oracle_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML10 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/markov_default.py \
          --config_seq.seq_model.n_layer=1 \
          --config_seq.seq_model.is_oracle=True \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml10_oracle_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml10_markov_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML10/ml10_markov_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML10 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/markov_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml10_markov_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  # ============================== ML45 ==============================
  - name: ml45_mate_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML45/ml45_mate_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML45 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/mate_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml45_mate_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml45_gpt_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML45/ml45_gpt_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML45 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/gpt_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml45_gpt_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml45_lstm_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML45/ml45_lstm_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML45 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/lstm_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml45_lstm_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml45_splagger_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML45/ml45_splagger_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML45 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/splagger_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml45_splagger_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml45_oracle_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML45/ml45_oracle_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML45 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/markov_default.py \
          --config_seq.seq_model.n_layer=1 \
          --config_seq.seq_model.is_oracle=True \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml45_oracle_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

  - name: ml45_markov_s0
    sku: 1x32G1-V100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - |
        RESUME_DIR=${BASE_DIR}/Metaworld/ML45/ml45_markov_s0_$$AMLT_EXPERIMENT_NAME
        if [ -f "$$RESUME_DIR/training_checkpoint.pth" ]; then
          echo "[INFO] Resuming from $$RESUME_DIR"
          RESUME_FLAG="--resume $$RESUME_DIR"
        else
          echo "[INFO] Fresh scratch run (no checkpoint at $$RESUME_DIR)"
          RESUME_FLAG=""
        fi
        conda run --no-capture-output --name mate \
          python main.py \
          --config_env=configs/envs/metaworld.py \
          --config_env.env_name=ML45 \
          --config_env.n_env=32 \
          --config_env.max_episode_steps=300 \
          --config_env.visualize_env=False \
          --config_rl=configs/rl/sac_default.py \
          --config_seq=configs/seq_models/markov_default.py \
          --config_seq.seq_model.n_layer=1 \
          --train_episodes=250000 \
          --seed=0 \
          --device=0 \
          --run_name=ml45_markov_s0 \
          --timestamp=$$AMLT_EXPERIMENT_NAME \
          --save_dir=${BASE_DIR} \
          $$RESUME_FLAG

