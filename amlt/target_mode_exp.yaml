description: >
  An experiment to evaluate the effect of keeping the target network in train vs eval mode. 
  Environments: ant-dir, walker-param, tmaze-active (T=200 and T=400), each with 3 random seeds.

env_defaults:
  NODES: 1
  GPUS: 1
  BASE_DIR: /mnt/v-hihwang/projects/mate

target:
  service: sing
  workspace_name: wsgcrrbt
  name: msrresrchvc

storage:
  my_storage:
    storage_account_name: azsussc
    container_name: v-hihwang
    mount_dir: /mnt/v-hihwang

environment:
  image: amlt-sing/acpt-torch2.7.1-py3.10-cuda12.6-ubuntu22.04
  setup:
    - conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main || true
    - conda create -y -n mate python=3.10
    - conda run --name mate conda install -y -c conda-forge mesa libstdcxx-ng
    - conda run --name mate pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu126
    - conda run --name mate pip install -r requirements.txt

code:
  local_dir: $CONFIG_DIR/..

jobs:
  # ──────────────────────────────────────────────
  # ant-dir × 3 seeds
  # ──────────────────────────────────────────────
  # Anchors (&common_env, &setup_cmd) are defined on the first job and reused by all subsequent jobs.
  - name: ant_dir_s0
    sku: 1x80G1-A100 # Just use whatever GPU is available
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: &common_env
        AMLT_DOCKERFILE_TEMPLATE: default
        SHARED_MEMORY_PERCENT: 0.5
        WANDB_API_KEY: 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
        PYTHONUNBUFFERED: "1"
    command:
      - &setup_cmd "export LD_LIBRARY_PATH=/home/aiscuser/.conda/envs/mate/lib:$$LD_LIBRARY_PATH"
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name ant-dir
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --train_episodes 160000
          --seed 0
          --device 0
          --run_name mate_target_train_s0 
          --save_dir ${BASE_DIR}

  - name: ant_dir_s1
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name ant-dir
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --train_episodes 160000
          --seed 1
          --device 0
          --run_name mate_target_train_s1
          --save_dir ${BASE_DIR}

  - name: ant_dir_s2
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name ant-dir
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --train_episodes 160000
          --seed 2
          --device 0
          --run_name mate_target_train_s2
          --save_dir ${BASE_DIR}

  # ──────────────────────────────────────────────
  # walker-param × 3 seeds
  # ──────────────────────────────────────────────
  - name: walker_param_s0
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name walker-param
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_target_train_s0
          --save_dir ${BASE_DIR}

  - name: walker_param_s1
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name walker-param
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --train_episodes 80000
          --seed 1
          --device 0
          --run_name mate_target_train_s1
          --save_dir ${BASE_DIR}

  - name: walker_param_s2
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name walker-param
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --train_episodes 80000
          --seed 2
          --device 0
          --run_name mate_target_train_s2
          --save_dir ${BASE_DIR}

  # ──────────────────────────────────────────────
  # tmaze-active T=200 × 3 seeds
  # ──────────────────────────────────────────────
  - name: tmaze_active_200_s0
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 200
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.max_norm 0.03
          --config_seq.seq_model.hidden_size 128
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_target_train_s0
          --save_dir ${BASE_DIR}

  - name: tmaze_active_200_s1
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 200
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.max_norm 0.03
          --config_seq.seq_model.hidden_size 128
          --train_episodes 80000
          --seed 1
          --device 0
          --run_name mate_target_train_s1
          --save_dir ${BASE_DIR}

  - name: tmaze_active_200_s2
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 200
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.max_norm 0.03
          --config_seq.seq_model.hidden_size 128
          --train_episodes 80000
          --seed 2
          --device 0
          --run_name mate_target_train_s2
          --save_dir ${BASE_DIR}

  # ──────────────────────────────────────────────
  # tmaze-active T=400 × 3 seeds  (A100 40GB)
  # ──────────────────────────────────────────────
  - name: tmaze_active_400_s0
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 400
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.max_norm 0.03
          --config_seq.seq_model.hidden_size 128
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_target_train_s0
          --save_dir ${BASE_DIR}

  - name: tmaze_active_400_s1
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 400
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.max_norm 0.03
          --config_seq.seq_model.hidden_size 128
          --train_episodes 80000
          --seed 1
          --device 0
          --run_name mate_target_train_s1
          --save_dir ${BASE_DIR}

  - name: tmaze_active_400_s2
    sku: 1x40G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 400
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.max_norm 0.03
          --config_seq.seq_model.hidden_size 128
          --train_episodes 80000
          --seed 2
          --device 0
          --run_name mate_target_train_s2
          --save_dir ${BASE_DIR}
