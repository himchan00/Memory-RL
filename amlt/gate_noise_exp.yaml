description: >
  Gate noise ablation: mate_gate (gate only, noise_std=0) vs mate_gate_noise (gate + noise, noise_std=0.5)
  Environments: ML45, ant-dir, walker-param, TMaze active T=400, TMaze active T=200

env_defaults:
  BASE_DIR: /mnt/v-hihwang/projects/mate

target:
  service: sing
  workspace_name: wsgcrrbt
  name: msrresrchvc

storage:
  my_storage:
    storage_account_name: azsussc
    container_name: v-hihwang
    mount_dir: /mnt/v-hihwang

environment:
  image: amlt-sing/acpt-torch2.7.1-py3.10-cuda12.6-ubuntu22.04
  setup:
    - conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main || true
    - conda create -y -n mate python=3.10
    - conda run --name mate conda install -y -c conda-forge mesa libstdcxx-ng
    - conda run --name mate pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu126
    - conda run --name mate pip install -r requirements.txt

code:
  local_dir: $CONFIG_DIR/..

jobs:
  # ══════════════════════════════════════════════
  # ML45 (200k episodes, hidden_size=256, n_layer=1)
  # ══════════════════════════════════════════════
  - name: ml45_gate
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: &common_env
        AMLT_DOCKERFILE_TEMPLATE: default
        SHARED_MEMORY_PERCENT: 0.5
        WANDB_API_KEY: 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
        PYTHONUNBUFFERED: "1"
    command:
      - &setup_cmd "export LD_LIBRARY_PATH=/home/aiscuser/.conda/envs/mate/lib:$$LD_LIBRARY_PATH"
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/metaworld.py
          --config_env.env_name ML45
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.0
          --train_episodes 200000
          --seed 0
          --device 0
          --run_name mate_gate
          --save_dir ${BASE_DIR}

  - name: ml45_gate_noise
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/metaworld.py
          --config_env.env_name ML45
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.5
          --train_episodes 200000
          --seed 0
          --device 0
          --run_name mate_gate_noise
          --save_dir ${BASE_DIR}

  - name: ml45_mean
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/metaworld.py
          --config_env.env_name ML45
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --train_episodes 200000
          --config_seq.seq_model.use_gate=False
          --config_seq.seq_model.gate_noise_std 0.0
          --seed 0
          --device 0
          --run_name mate_mean
          --save_dir ${BASE_DIR}

  # ══════════════════════════════════════════════
  # ant-dir (160k episodes)
  # ══════════════════════════════════════════════
  - name: ant_dir_gate
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name ant-dir
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.0
          --train_episodes 160000
          --seed 0
          --device 0
          --run_name mate_gate
          --save_dir ${BASE_DIR}

  - name: ant_dir_gate_noise
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name ant-dir
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.5
          --train_episodes 160000
          --seed 0
          --device 0
          --run_name mate_gate_noise
          --save_dir ${BASE_DIR}

  - name: ant_dir_mean
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name ant-dir
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=False
          --config_seq.seq_model.gate_noise_std 0.0
          --train_episodes 160000
          --seed 0
          --device 0
          --run_name mate_mean
          --save_dir ${BASE_DIR}

  # ══════════════════════════════════════════════
  # walker-param (160k episodes)
  # ══════════════════════════════════════════════
  - name: walker_param_gate
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name walker-param
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.0
          --train_episodes 160000
          --seed 0
          --device 0
          --run_name mate_gate
          --save_dir ${BASE_DIR}

  - name: walker_param_gate_noise
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name walker-param
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.5
          --train_episodes 160000
          --seed 0
          --device 0
          --run_name mate_gate_noise
          --save_dir ${BASE_DIR}

  - name: walker_param_mean
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/mujoco.py
          --config_env.env_name walker-param
          --config_env.visualize_env=False
          --config_rl configs/rl/sac_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=False
          --config_seq.seq_model.gate_noise_std 0.0
          --train_episodes 160000
          --seed 0
          --device 0
          --run_name mate_mean
          --save_dir ${BASE_DIR}

  # ══════════════════════════════════════════════
  # TMaze active T=400 (80k episodes, DQN, hidden_size=128)
  # ══════════════════════════════════════════════
  - name: tmaze_active_400_gate
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 400
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.0
          --config_seq.seq_model.hidden_size 128
          --config_seq.max_norm 0.03
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_gate
          --save_dir ${BASE_DIR}

  - name: tmaze_active_400_gate_noise
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 400
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.5
          --config_seq.seq_model.hidden_size 128
          --config_seq.max_norm 0.03
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_gate_noise
          --save_dir ${BASE_DIR}


  - name: tmaze_active_400_mean
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 400
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=False
          --config_seq.seq_model.gate_noise_std 0.0
          --config_seq.seq_model.hidden_size 128
          --config_seq.max_norm 0.03
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_mean
          --save_dir ${BASE_DIR}


  # ══════════════════════════════════════════════
  # TMaze active T=200 (80k episodes, DQN, hidden_size=128)
  # ══════════════════════════════════════════════
  - name: tmaze_active_200_gate
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 200
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.0
          --config_seq.seq_model.hidden_size 128
          --config_seq.max_norm 0.03
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_gate
          --save_dir ${BASE_DIR}

  - name: tmaze_active_200_gate_noise
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 200
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=True
          --config_seq.seq_model.gate_noise_std 0.5
          --config_seq.seq_model.hidden_size 128
          --config_seq.max_norm 0.03
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_gate_noise
          --save_dir ${BASE_DIR}


  - name: tmaze_active_200_mean
    sku: 1x80G1-A100
    process_count_per_node: 1
    sla_tier: Premium
    execution_mode: basic
    submit_args:
      env: *common_env
    command:
      - *setup_cmd
      - conda run --no-capture-output --name mate
          python main.py
          --config_env configs/envs/tmaze_active.py
          --config_env.env_name 200
          --config_rl configs/rl/dqn_default.py
          --config_seq configs/seq_models/mate_default.py
          --config_seq.seq_model.use_gate=False
          --config_seq.seq_model.gate_noise_std 0.0
          --config_seq.seq_model.hidden_size 128
          --config_seq.max_norm 0.03
          --train_episodes 80000
          --seed 0
          --device 0
          --run_name mate_mean
          --save_dir ${BASE_DIR}